const CURRENT_LINE = 'current_line'
const ERROR = 'error_line'
const NEXT_LINE = 'next_line'
const PROMPT = 'prompt_line'
const WARNING = 'warning_line'

export default {
    CURRENT_LINE,
    ERROR,
    NEXT_LINE,
    PROMPT,
    WARNING,
}